{
    "type": "CompositeLens",
    "params": [
        {
            "x": ..., "y": ..., "angle": ..., "factor": ...,
            "lens": {
                "type": "PlummerLens",
                "params": ...
            }
        },
        ...
    ]
}

###

{
    "type": "PlummerLens",
    "params": {
        "mass": 1e14*MASS_SUN, # voor vaste waarde
        "mass": [ 1e14*MASS_SUN ], # voor voorgedef pct rond deze waarde
        "mass": [ 1e14*MASS_SUN, 0.1 ], # voor dit pct rond startwaarde
        "mass": { "initmin": ... , "initmax": ..., "hardmin": ..., "hardmax": ... },
        "width": ...
    }
}

# Mapping kennen mass -> mass_scaled, width -> width_scaled

{
    "type": "MultiplePlummerLens",
    "params": [
        { "mass": ... , "width": ..., "x": ..., "y": ... },
        # etc
    ]
}
